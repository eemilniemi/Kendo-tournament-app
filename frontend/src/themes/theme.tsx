import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#D01C1C"
    },
    secondary: {
      main: "#9b9484"
    }
  },
  typography: {
    fontSize: 16
  }
});

export default theme;
